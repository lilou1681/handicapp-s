<%= simple_form_for([@child, @reminder]) do |f| %>
  <%= f.input :name, label: false, placeholder: "Nom"%>
  <%= f.input :date, html5: true, label: false %>
  <%= f.button :submit, "Ajouter un rappel", class: "btn btn-orange" %>
<% end %>
